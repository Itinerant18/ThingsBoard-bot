# Chat Profile - Chat Query Endpoint
# Expose HTTP endpoints
spring.main.web-application-type=servlet
server.port=8080

# Disable RabbitMQ listeners on this node
spring.rabbitmq.listener.simple.auto-startup=false
