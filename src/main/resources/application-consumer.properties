# Consumer Profile - Background Worker
# Disable HTTP web server entirely for safety and performance
spring.main.web-application-type=${APP_WEB_TYPE:none}

# Enable RabbitMQ listeners on this node
spring.rabbitmq.listener.simple.auto-startup=true
