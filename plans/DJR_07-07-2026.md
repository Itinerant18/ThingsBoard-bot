DJR — Ganesh Singha — 07-07-2026

Product/Module: SAI (Smart Assistant for IoT) — ThingsBoard IoT Bot — Phase 2: Multi-Intent Orchestrator, Format-Aware Generation & Active Mode (Steps 4-6)

Today's Work:

1. Built the Multi-Intent Orchestrator (Task 2.2)
- Implemented `MultiIntentOrchestrator`: executes the extractor's intent list (already capped at 3) against the existing `AnswerHandler` chain without modifying any handler. Intents below the configured confidence gate are dropped; every entity resolves through the Phase 1 fuzzy bands before dispatch.
- Locked the safety semantics: an entity in the confirmation/suggestion band halts the entire reply into a clarification (never a partial answer next to a possibly-wrong branch), a below-floor entity produces "couldn't find that branch" instead of a fabricated answer, and an empty/all-gated extraction falls back to `UNANSWERED` so the caller defers to the LLM path.
- Added `responseFormat` to `ResolvedQuery` for the orchestrator to thread through. Tests run mock-free against the real fixture snapshots and the real handler chain (single intent, multi-intent combined sections, typo entity, unknown entity, confidence gating, global overview, unhandled intent).

2. Wired Format-Aware Generation (Task 2.3)
- Added `ResponseFormatInstructions`: maps each `ResponseFormat` value onto one instruction line appended to the LLM user message (both branch-scoped and global prompt variants). Null format adds nothing, so every existing prompt stays byte-identical until active mode populates the field.
- Deterministic handlers keep their fixed templates for now — rendering hints there are a documented no-op deferred to the Phase 3 handler work, avoiding a rewrite of all 14 handlers in this phase.

3. Wired Active Mode and Built the Deterministic Extractor Bench (Task 4.1/4.2 style)
- Wired `iotchatbot.extractor.mode=active` into `ChatService`: the extractor now drives only what the deterministic resolver could not place (`GENERAL_LLM` / ambiguous). `REFUSAL` and `OUT_OF_SCOPE` classifications get canned replies (injection attempts are logged), everything else runs through the orchestrator, and `UNANSWERED` falls back to the existing LLM path carrying the requested format. The deterministic fast path is untouched in every mode; the flag default stays `off`.
- Built `RecordedIntentExtractor` — replays `fixtures/extractor_recordings.json` through the production parser, giving the bench structured-JSON assertions (per the master plan's Task 4.2 principle: assert on extractor output, not final prose) with zero tokens and zero network calls.
- Added bench scenarios: paraphrase classification, multi-intent (two combined answer sections), follow-up intent inheritance ("and Liluah?"), out-of-scope detection, prompt-injection refusal, and format survival with a typo entity resolved through the fuzzy bands.

4. Validated the Complete Phase 2 Implementation
- Ran the full regression suite after every step; ended at 320 tests, 0 failures (1 skipped Docker-only Postgres test). All six Phase 2 steps shipped across six reviewed, individually pushed commits.

Status: Completed ✅ (Phase 2 — extractor contracts, LLM extraction, shadow mode, multi-intent orchestration, format-aware generation, and active-mode bench coverage — fully implemented, regression-covered, and pushed).

Blockers (if any): None.

Tomorrow's Plan:
- Begin Phase 3: static glossary path (Task 3.2) and canned capability replies for `HOW_TO`/`NAVIGATION` (descoped from Docs-RAG per architecture review — the bot is the sole dashboard interface).
- Add the prompt-injection pre-filter ahead of the extractor call (Task 3.3), layered in front of the extractor-level `REFUSAL` classification already shipped.

Hours Worked: [8 hrs]
