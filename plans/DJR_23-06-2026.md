DJR — Ganesh Singh — 23-06-2026
Product/Module: SAI (Smart Assistant for IoT) — ThingsBoard IoT Bot — Sub-Device Field Answers, Capability Gap Analysis & Implementation Planning

Today's Work:

1. Implemented Sub-Device Field-Level Status Answers
The bot earlier gave a single generic answer for any sub-device question (e.g. "CCTV power status" and "CCTV log status" both returned the same roll-up). Reworked it so a question that names a sub-device AND a specific field (power / system / log / health status) is answered from that exact field's value — for all six sub-devices (CCTV, IAS, BAS, FAS, Time Lock, Access Control), across all banks. Values are shown as-is, including "N/A". When no specific field is named, the existing roll-up answer is preserved. Added tests; full suite green (192 tests).

2. Capability Gap Analysis Across All Dashboards
Reviewed the full question set (~1,150 questions across 8 dashboards — Home, Hierarchy Health, Branch List, Device List, Alarm Center, S-Vault, Branch/Uptime/Recording/CCTV-Inventory reports). Traced each family against the live code and data and classified them into: answers well, answers wrong/partial, and will never answer — with the root cause for each (data missing vs. code/context gap vs. out-of-scope workflow).

3. Confirmed the Production Data Path
Traced how the bot fetches data in production: device attributes are read from Redis (the live device-state cache, populated from TimescaleDB via the replay job), while branch identity/hierarchy comes from the database. This clarified that most "wrong/partial" answers are code gaps, not missing data — the values already reach Redis.

4. Re-validated the CCTV Inventory Gap (Data Is Present)
Checked the backup and confirmed vendor, model, HDD slots, storage capacity (TB) and resolution are all present in the data already flowing to Redis. Revised the earlier assessment from "not possible (data gap)" to "achievable with a read-side code change". Also identified a real correctness bug: the existing HDD-info handler only reads the Hikvision JSON shape, so Dahua devices return N/A.

5. Investigated IMEI Data Quality
Surveyed IMEI across all 146 devices: 112 reported, 34 missing — where "missing" appears in four shapes ("0", "None", absent key, and a nested dev_id object). Defined a normalization rule so the bot answers honestly ("IMEI not reported (missing)") instead of echoing raw "0"/"None", matching the dashboard's own missing-IMEI classification.

6. Authored Two Implementation Plans
Wrote step-by-step plans: (a) the new CCTV_INVENTORY_INFO feature, and (b) closing all the "wrong/partial" gaps (IMEI, GPS, last-reported/staleness, network operator, hierarchy navigation, cross-branch ranking/compare, multi-condition filters, category health %), including a feasibility verdict and sequencing.

Status: Completed ✅ (sub-device field feature implemented and tested — full suite 192 green; analysis and both plans documented).

Blockers (if any): None.

---

Other Task (not part of this project):

7. Hestia & Jarvis Dashboards — Mobile-View Redesign to Match New BOI Dashboard
Completed the mobile-view redesign of the old Hestia and Jarvis dashboards to align with the new Bank of India dashboard design (mobile layout, components, and styling brought in line with the new look).

Status: Completed ✅

---

Tomorrow's Plan:
- Implement the CCTV_INVENTORY_INFO intent (vendor / model / HDD slots / storage / resolution) and fix the Dahua HDD-schema handler.
- Begin the quick-win gap fixes: restore + harden IMEI answering (with the missing-value normalization rule), then GPS, last-reported, and network operator.

Hours Worked: [8 hrs]
