DJR — Ganesh Singha — 26-06-2026

Product/Module: SAI (Smart Assistant for IoT) — ThingsBoard IoT Bot — Deployments, Data Sync, and Diagnostics

Today's Work:

1. Resolved RabbitMQ Queue Backlog & Initialization Race Condition
Fixed the CloudAMQP queue initialization issue. Modified RabbitMQQueueService.java to declare and initialize queues on Spring's ApplicationReadyEvent instead of @PostConstruct. This resolved the startup boot-order race condition, successfully draining the backed-up queues on EC2.

2. Fetched Fresh ThingsBoard Backup
Executed the tb-backup Maven utility to query and pull the latest device telemetry and attributes (SERVER_SCOPE, CLIENT_SCOPE, and SHARED_SCOPE) for all 147 devices directly from ThingsBoard. This updated the local thingsboard_devices_backup.json database snapshot.

3. Database Migration & Cache Sync
- Wiped and populated TimescaleDB using the tb-import Maven utility, refreshing the device_events, hierarchy_nodes, and branch_ancestor_paths tables with the updated 147-device snapshot and dynamically rebuild the 217-node customer hierarchy.
- Rebuilt the Upstash Redis cache by invoking the replay API endpoint (POST /api/v1/admin/replay?customerId=ALL) with a wide time window (2020–2035) to handle any timestamp timezone shifts, updating all customer caches and online state counters.

4. Diagnosed Hierarchy Mapping Issues (BRANCH BAGUIHATI TEGHORIA)
Investigated why "BRANCH BAGUIHATI TEGHORIA" was appearing under the "Other" group. Found that the device has mismatched attributes configured in ThingsBoard:
- branch_name: "BRANCH BAGUIHATI TEGHORIA"
- full_path: "HO(Bank Of India) → NBG EAST → ZO BARASAT → BRANCH BAGUIHATI"
Because the path leaf name doesn't match the branch name, the GlobalOverviewHandler is unable to link the branch to its parent zone, defaulting it to "Other".

5. Analyzed Webhook Warning Logs
Diagnosed logs regarding HMAC and dropped payloads:
- Explained that HMAC verification warnings occur when the environment variable security secret (webhook-hmac-secret) is unset.
- Explained that "Invalid event payload (no message id), dropping" warnings occur normally when ThingsBoard webhooks send data that matches existing state (no telemetry/attribute changes), resulting in empty parsed payloads.

Status: Completed ✅ (Deployment resolved, fresh data import and Redis sync completed, issues diagnosed).

Blockers (if any): None.

Tomorrow's Plan:
- Align with the operations team to fix the ThingsBoard configuration mismatch (branch_name vs full_path) for BOI-BAGUIHATI.
- Continue monitoring EC2 queue ingestion logs.

Hours Worked: [8 hrs]
