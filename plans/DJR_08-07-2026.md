DJR — Ganesh Singha — 08-07-2026

Product/Module: SAI (Smart Assistant for IoT) — ThingsBoard IoT Bot — Phase 3: Glossary, Canned Capability Replies & Safety Gates

Today's Work:

1. Planned and Descoped Phase 3 Architecture
- Authored the Phase 3 implementation plan (`phase3_glossary_safety_plan.md`): closed the gap where the five knowledge intents locked in Phase 0 (`GLOSSARY`, `CONCEPT_EXPLAIN`, `HOW_TO`, `NAVIGATION`, `TROUBLESHOOTING`) were classified but unhandled, falling through to free-generation on the LLM path.
- Confirmed Docs-RAG stays cut per the earlier architecture review (the bot is the sole dashboard interface) - no embeddings, no vector store, no document retrieval anywhere in the system.

2. Built the Static Glossary Path (Task 3.2)
- Authored `glossary.json` - 23 domain terms with aliases (IAS, BAS, FAS, TLS, ACS, NBG, ZO, offline, stale, telemetry, heartbeat, gateway, tamper, HDD error, mains, battery low, uptime, fault-vs-alarm, NVR, DVR, SOL ID, hierarchy), operator-editable without a rebuild.
- Implemented `GlossaryService` (longest-first word-boundary term scanning, alias/full-name lookup) and `GlossaryHandler` for `GLOSSARY`/`CONCEPT_EXPLAIN` - answers strictly from the file; an unknown term gets an honest decline, never an invented definition.
- Added keyword detection in `QueryIntentResolver` so glossary/concept questions work in every extractor mode (not only behind the LLM), checked before any data-keyword branch and gated on the term actually existing in the glossary so data questions are never stolen. Added `QueryIntent.isKnowledge()` and exempted knowledge intents from the "which branch?" ambiguity flag.

3. Built Canned Capability Replies (descoped Task 3.1)
- Implemented `CapabilityReplyHandler` for `HOW_TO`/`NAVIGATION`/`TROUBLESHOOTING`, loading fixed, honest replies from `capability-replies.properties` - the bot states its read-only, interface-only role and redirects to concrete example questions instead of hallucinating UI steps that don't exist.
- Fixed an integration gap in `MultiIntentOrchestrator`: it was fuzzy-resolving every extracted entity as a branch name, which would have turned a glossary term like "stale" into a bogus "couldn't find a branch named stale" clarification. Knowledge-intent entities now skip branch resolution entirely.

4. Shipped the Pre-LLM Safety Gate (Task 3.3)
- Built `SafetyGateService`, wired into `ChatService` before the router, resolver, extractor, or any LLM call - injection attempts and garbage input are stopped at zero token cost.
- Injection screen: regex markers for ignore-instructions phrasings, system-prompt extraction verbs, persona switches, developer/DAN mode, delimiter smuggling, and rule-override language.
- Garbage screen: blank/symbol-only input, repeated-character runs, and keyboard-mash detection via consonant-run length (tuned mid-build after the initial vowel-absence heuristic missed "asdfghjkl").
- Pinned two release-gate corpora in tests: a 22-attack injection corpus that must block 100%, and an 11-phrase false-positive corpus ("how do I ignore a false alarm", "system prompt response time", bare branch codes) that must pass 100% - both green. This is the first of three independent defense layers, ahead of the extractor's REFUSAL classification and the existing generation-call prompt guard.

5. Extended Both Regression Benches and Validated
- Added glossary, concept-explain, and capability scenarios to the golden-question bench (keyword path, works in every mode) and to the extractor-recording replay bench, plus a defense-in-depth check that the pre-gate blocks the recorded injection string before any extractor call would run.
- Registered `GlossaryHandler` and `CapabilityReplyHandler` into the default handler set. Ran the complete suite after every step; ended at 349 tests, 0 failures (1 skipped Docker-only Postgres test). All four Phase 3 steps shipped across four reviewed, individually pushed commits.

Status: Completed ✅ (Phase 3 - glossary, capability replies, safety gate, and bench coverage - fully implemented, regression-covered, and pushed).

Blockers (if any): None.

Tomorrow's Plan:
- Begin Phase 4/5 groundwork: review shadow-mode agreement metrics once real traffic accumulates, and start threshold calibration (fuzzy bands, extractor confidence gate) against the regression bench.
- Prepare user documentation and deployment notes ahead of the eventual active-mode cutover.

Hours Worked: [8 hrs]
