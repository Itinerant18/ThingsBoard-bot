DJR — Ganesh Singha — 04-07-2026

Product/Module: SAI (Smart Assistant for IoT) — ThingsBoard IoT Bot — Phase 1: Local Branch Dictionary & Fuzzy Resolver

Today's Work:

1. Implemented the Local Canonical Branch Dictionary (Task 1.1)
- Built `BranchDictionary` — an immutable per-customer index of branch name variants (exact, normalized, compact) reusing the existing `BranchAliasIndex` normalization so exact and fuzzy layers agree on spelling.
- Built `BranchDictionaryService` — caches one dictionary per customer in memory from PostgreSQL hierarchy leaf nodes and refreshes on a configurable schedule, so lookups never hit the database. Customer scoping is enforced at build time (a BOI query can never resolve a CANARA branch).
- Analyzed the real `thingsboard_devices_backup.json` export (147 devices: 104 BOI, 11 SBI, 10 BOB, 5 CANARA, plus test entries) to confirm dictionary cardinality, multi-bank tenancy, and the `sol_id_change` rename history (e.g. `BOI-DX7` -> `BOI-MALDATOWN`) the resolver must handle.

2. Built the Manual Alias Table (Task 1.4)
- Externalized shorthand overrides into `branch-aliases.properties` (`BBSR` -> `Bhubaneswar`, `MT` -> `Malda Town`, `HO` -> `Head Office`) so operators can extend the alias list without recompiling.
- Wired the table into dictionary enrichment so shorthand lookups resolve to the correct canonical branch.

3. Finalized the Fuzzy Branch Resolver and Confidence Bands (Tasks 1.2-1.3)
- Implemented the Jaro-Winkler matching service (Apache `commons-text`) with the three confidence bands: >0.90 resolves silently, 0.75-0.90 returns a "Did you mean X?" confirmation, and below that the top-3 suggestions.
- Tuned the suggestion floor from 0.40 to 0.55 after observing garbage input (e.g. `QXZPLW999`) leaking into suggestions — garbage now returns a clean NO_MATCH, in line with the zero-fabrication release gate.
- Exposed all three thresholds as configuration properties (`iotchatbot.fuzzy.*`) so Phase 5 calibration can tune them without code changes.

4. Wired Fuzzy Fallback into Query Resolution
- `QueryIntentResolver` now scores 1-3 word windows of the question through the fuzzy resolver whenever exact alias matching fails and the query would otherwise be flagged ambiguous. High-confidence hits resolve the branch silently — typos like "Tarakeswar" and "Lilua" now answer correctly.
- Mid/low band outcomes ride along on `ResolvedQuery`, and `ChatService` renders band-aware clarifications ("Did you mean **X**?" / top-3 options) instead of dumping the entire branch list. The exact-match fast path is untouched, preserving all existing behavior.

5. Extended the Regression Bench and Validated the Full Suite
- Added typo-resolution scenarios to `golden_questions.json` ("Tarakeswar", "Lilua", "Bhadreswer") asserting intent, resolved branch, and answer content end-to-end.
- Ran the complete test suite: 273 tests, 0 failures (1 skipped Docker-only Postgres test). Phase 1 shipped across five reviewed commits.

Status: Completed ✅ (Phase 1 — dictionary, alias table, fuzzy resolver, resolution wiring, and bench coverage — fully implemented, regression-covered, and pushed).

Blockers (if any): None.

Tomorrow's Plan:
- Begin Phase 2: draft the LLM JSON extractor prompt (intent list + entities + format + confidence schema).
- Design the multi-intent orchestrator loop over the existing `AnswerHandler` beans with a cap of 3 intents per query.

Hours Worked: [8 hrs]
