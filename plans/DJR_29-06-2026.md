DJR — Ganesh Singha — 29-06-2026

Product/Module: SAI (Smart Assistant for IoT) — ThingsBoard IoT Bot — Diagnostics, Matching Fixes, and Architecture Analysis

Today's Work:

1. Resolved Branch Name Matching in Comparison Queries
Diagnosed and fixed a bug where branch comparison queries failed when branch names were typed without spaces (e.g., `"BOI-MALDATOWN"` vs database display name `"BRANCH MALDA TOWN"`). Modified FleetAnalyticsHandler.java to check all generated branch aliases (which contain compact, hyphenated, and space-stripped variants) instead of just the clean display name.

2. Expanded Test Suite Coverage & Graph Update
Added the `shouldCompareTwoBranchesWithAliases` unit test in DeterministicAnswerServiceTest.java to verify alias-based comparison lookups. Ran the full Maven test suite, resulting in 248/248 passing tests. Updated the code knowledge graph using `graphify update .`.

3. Conducted Query Capability Audit
Mapped out the exact telemetry data fields, Redis keys, and attributes in thingsboard_devices_backup.json to the bot's 43 deterministic `QueryIntent` enums. Documented exactly what questions the bot can answer (CCTV DVR/HDD, Gateways, Power/Battery health, Access Control/Time Lock subsystems, and dynamic alert aggregation).

4. Documented Deterministic vs. RAG Architectural Challenges
Prepared a detailed report outlining the trade-offs of the current hybrid deterministic router compared to traditional RAG (focusing on synonym sensitivity, typo brittleness, compound queries, strict templating, and code maintenance overhead). Saved it to the plans directory as deterministic_vs_rag_challenges.md.

5. Provided Core AI Concepts Brainstorming
Drafted educational summaries of vector databases, text embeddings, and semantic routing strategies (including fuzzy Jaro-Winkler string distance, LLM pre-parsers, and embedding similarity thresholds) to support future architectural decisions.

Status: Completed ✅

Blockers (if any): None.

Tomorrow's Plan:
- Discuss the fuzzy matching and LLM pre-parser strategies (Options 1, 2, and 3) with the team to prioritize the next steps for enhancing the bot's spelling tolerance.

Hours Worked: [8 hrs]
