DJR — Ganesh Singha — 09-07-2026

Product/Module: SAI (Smart Assistant for IoT) — ThingsBoard IoT Bot — Phase 4: Test Bench Runner & Release Scorecard, Phase 5: Threshold Calibration & Handoff Docs

Today's Work:

1. Audited Phase 4 Against the Master Plan
- Reviewed the regression coverage built continuously since Phase 1 (bench-first approach) against the master plan's explicit Phase 4 deliverables and found two gaps: no standalone test runner (scenarios were scattered across individual JUnit test methods) and no computed scorecard - test pass/fail existed, but no aggregate percentage gated against the release thresholds.

2. Built the Test Bench Runner (Task 4.1)
- Implemented `TestBenchRunner`: loads every scenario source from JSON fixtures - golden questions (keyword path), extractor replay scenarios (recorded extraction through the orchestrator), and the safety corpora - and drives the full offline pipeline against each. Adding a new test case is now a fixture edit, not a Java method.
- Tagged every golden-question scenario with a `category` field (paraphrase, entity_resolution, knowledge) and added a dedicated multi-intent extractor scenario file (`bench_extractor_scenarios.json`) plus a second multi-intent recording.
- Consolidated the safety corpora (injection, false-positive, garbage) into one shared fixture (`bench_safety_corpus.json`) so the unit test and the bench scorecard can never drift apart - repointed `SafetyGateServiceTest` at it.

3. Built the Release Scorecard (Task 4.3)
- Implemented `BenchScorecard`: aggregates per-category pass rates and compares them against the master plan's gating table (paraphrase >=95%, entity resolution >=98%, multi-intent >=90% non-blocking, and 100% blocking gates for injection, garbage, false-positive, and knowledge).
- Implemented `ReleaseGateTest`, which prints the full scorecard table to the build log and fails the build if any blocking category is below threshold - the ship/no-ship answer, computed automatically rather than eyeballed. Current result: 7 categories, 76 scenarios, all PASS.

4. Ran Phase 5 Task 5.1 - Offline Threshold Calibration
- Built `ThresholdCalibrationSweep`: grid-searches the fuzzy branch resolver's three confidence bands against two signals - recall (do typo'd entity-resolution scenarios still resolve to the correct branch?) and fabrication safety (does a fixed corpus of made-up branch names ever silently resolve?).
- Result: the shipped defaults (0.90/0.75/0.55) hold 100% recall with zero fabrication leaks and have margin in both directions - tightening to 0.95 breaks recall (85.7%), loosening to 0.80 still holds 100%/0 leaks. No change made to shipped defaults; findings written to `phase5_calibration_and_handoff.md`.
- Documented that the other half of calibration (extractor confidence-gate tuning, off/shadow/active cutover decision) cannot be completed offline - it requires `extractor.agreement` metrics from shadow mode running against real production traffic, which is not yet deployed. Wrote the exact procedure to run once it is.

5. Wrote Phase 5 Task 5.2 - Handoff Documentation
- `user_guide.md`: what SAI answers by category with example questions, how branch-name typos/shorthand are handled, and what it deliberately will not do (no navigation, no changes, no fabrication, no instruction-following from user input).
- `deployment_guide.md`: every `iotchatbot.*` config flag introduced across Phases 1-5 with defaults and purpose, the off/shadow/active extractor cutover procedure with rollback, and a pre-deploy checklist tied to the release gate.
- `regression_report.md`: current scorecard snapshot, what each category proves, and how to extend the bench by editing fixtures rather than writing code.

6. Validated and Shipped
- Ran the full suite after every step; ended at 351 tests, 0 failures (1 skipped Docker-only Postgres test). Phase 4 and the completable half of Phase 5 shipped across three reviewed, individually pushed commits.

Status: Completed ✅ (Phase 4 fully implemented; Phase 5 complete except the deployment-blocked half of Task 5.1, which is explicitly documented rather than left silent).

Blockers (if any): Extractor confidence-gate calibration and the shadow-to-active cutover decision are blocked on a production deployment - cannot be completed from this environment. No other blockers.

Tomorrow's Plan:
- Await deployment to run the app in `iotchatbot.extractor.mode=shadow` against real traffic and begin collecting `extractor.agreement` data.
- In the meantime, grow the `entity_resolution` bench corpus as new real-world typo patterns are identified.

Hours Worked: [8 hrs]
