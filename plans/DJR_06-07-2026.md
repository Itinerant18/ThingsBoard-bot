DJR — Ganesh Singha — 06-07-2026

Product/Module: SAI (Smart Assistant for IoT) — ThingsBoard IoT Bot — Phase 2: LLM JSON Extractor & Shadow Mode (Steps 1-3)

Today's Work:

1. Planned and Designed Phase 2 Architecture
- Authored the Phase 2 implementation plan (`phase2_llm_extractor_plan.md`): extractor sits as a second layer behind the existing keyword+fuzzy resolver (Phase 1), only firing on questions that fall through to the LLM path today — zero regression risk, tokens spent only where already spent.
- Locked the cutover safety model: three extractor modes (`off` / `shadow` / `active`), shadow mode required before any user-facing cutover, gated on >=95% agreement with the deterministic resolver.

2. Built the Extractor Contracts and Fail-Closed Parser (Task 2.1)
- Defined `ExtractedIntent` / `ExtractionResult` DTOs and the `IntentExtractor` interface (so the regression bench can substitute a fixture-replay implementation later).
- Authored the extractor system prompt enumerating the full `QueryIntent` taxonomy, `ResponseFormat` values, verbatim-entity rule, follow-up inheritance via conversation history, and injection-refusal policy.
- Implemented `ExtractionResultParser`: malformed JSON returns empty (safe fallback), unknown intent/format values coerce to `OUT_OF_SCOPE` at confidence 0, confidence is clamped to [0,1], and output past 3 intents is truncated by confidence. The parser never throws. Covered with 13 unit tests.

3. Built the LLM-Backed Extractor Behind an Off-by-Default Flag (Task 2.1)
- Added `OpenAIClient.completeJson` — a generic deterministic JSON-mode completion (temperature 0, `response_format=json_object`) with conversation history, generalizing the existing judge-only `evaluateJson` pattern.
- Implemented `LlmIntentExtractor`: one call with the extractor prompt and the last N conversation turns (configurable), records `extractor.latency` / `extractor.result` metrics, and returns an empty result on any failure so extractor errors can never affect the chat pipeline.
- Introduced `ExtractorConfig` (`iotchatbot.extractor.mode`, default `off`) so nothing changes in production behavior yet.

4. Shipped Shadow Mode with Agreement Metrics (cutover safety)
- Built `ExtractorShadowService`: when mode is `shadow`, fires the extractor asynchronously right after deterministic resolution and only records whether the two agree (`extractor.agreement{match|intent_mismatch|entity_mismatch|empty}`). Runs on a small bounded daemon pool that drops work under pressure — zero impact on user-facing answers.
- Entity agreement compacts extracted entity strings onto the resolved branch's technical id/display name, so verbatim typos still count as agreement.
- Wired into `ChatService` immediately after query resolution. This is what produces the calibration data required before flipping to active mode.

5. Validated Across Three Commits
- Ran the full suite after each step; all green throughout (extractor parser, LLM extractor, shadow service, and `ChatService` wiring tests). Pushed three reviewed commits.

Status: Completed ✅ (Phase 2 steps 1-3 — contracts, LLM extractor, shadow mode — implemented, regression-covered, and pushed).

Blockers (if any): None.

Tomorrow's Plan:
- Begin Phase 3: static glossary path (Task 3.2) and canned capability replies for `HOW_TO`/`NAVIGATION` (descoped from Docs-RAG per architecture review — the bot is the sole dashboard interface).
- Add the prompt-injection pre-filter ahead of the extractor call (Task 3.3), layered in front of the extractor-level `REFUSAL` classification already shipped.

Hours Worked: [8 hrs]
